# Cliply Backend Environment Variables
# 
# Copy this file to .env.local and fill in your actual values.
# Never commit .env.local or any file containing real secrets.
#
# For detailed documentation, see ENV.md

# 
# Core
# 

# Node environment: development, test, or production
NODE_ENV=development

# 
# Supabase (Required)
# 

# Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (public, safe for client-side use)
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase service role key (private, server-side only - has full access)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# 
# Worker Configuration (Optional)
#

# Worker poll interval in milliseconds (how often worker checks for new jobs)
WORKER_POLL_MS=

# Worker heartbeat interval in milliseconds (how often worker updates heartbeat)
WORKER_HEARTBEAT_MS=

# Worker reclaim interval in milliseconds (how often to check for stale jobs)
WORKER_RECLAIM_MS=

# Worker stale job threshold in seconds (jobs older than this are considered stale)
WORKER_STALE_SECONDS=

# Log sample rate (0.0 to 1.0, controls percentage of logs to emit)
LOG_SAMPLE_RATE=1

#
# External Services (Optional)
#

# Sentry DSN for error monitoring (leave empty to disable)
SENTRY_DSN=

# Direct database connection URL (optional, Supabase connection is preferred)
DATABASE_URL=

# Stripe secret key for payment processing
STRIPE_SECRET_KEY=

# Stripe webhook secret for verifying webhook signatures
STRIPE_WEBHOOK_SECRET=

# Deepgram API key for transcription services
DEEPGRAM_API_KEY=

# OpenAI API key for AI features
OPENAI_API_KEY=

# 
# YouTube/Google OAuth (Optional for test, Required for production)
#

# Google OAuth client ID
GOOGLE_CLIENT_ID=

# Google OAuth client secret
GOOGLE_CLIENT_SECRET=

# YouTube OAuth redirect URL (must match Google Cloud Console configuration)
YOUTUBE_OAUTH_REDIRECT_URL=

#
# TikTok OAuth (Optional for test, Required for production)
#

# TikTok OAuth client ID
TIKTOK_CLIENT_ID=

# TikTok OAuth client secret
TIKTOK_CLIENT_SECRET=

# TikTok OAuth redirect URL (must match TikTok Developer Portal configuration)
TIKTOK_OAUTH_REDIRECT_URL=

# TikTok token URL (TikTok API endpoint for token exchange)
TIKTOK_TOKEN_URL=

# TikTok encryption key (Base64-encoded 32-byte key for encrypting tokens at rest)
# Generate with: openssl rand -base64 32
TIKTOK_ENCRYPTION_KEY=

#
# Cron & Automation (Optional)
#

# Secret key for securing cron job endpoints
CRON_SECRET=

# Secret for bypassing Vercel automation restrictions
VERCEL_AUTOMATION_BYPASS_SECRET=

#
# Next.js Public Variables (Client-Side)
# These are exposed to the browser and must be prefixed with NEXT_PUBLIC_
#

# Public Supabase URL (fallback if SUPABASE_URL not set)
NEXT_PUBLIC_SUPABASE_URL=

# Public Supabase anonymous key (fallback if SUPABASE_ANON_KEY not set)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Public TikTok redirect URL (for client-side OAuth flows)
NEXT_PUBLIC_TIKTOK_REDIRECT_URL=

# Public YouTube redirect URL (for client-side OAuth flows)

# Public app URL (used for OAuth redirects and client-side navigation)
NEXT_PUBLIC_APP_URL=

# Public Sentry DSN (for client-side error monitoring)
NEXT_PUBLIC_SENTRY_DSN=
